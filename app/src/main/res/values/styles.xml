<resources>

    <!-- Base application theme. -->
    <style name="VoizyTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/white</item>
        <item name="colorAccent">@color/voizy_orange</item>
        <item name="android:statusBarColor">@color/voizy_orange</item>
        <item name="actionBarStyle">@style/VoizyAppBarTheme</item>
    </style>

    <style name="VoizyAppBarTheme" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:background">@color/voizy_orange</item>
        <item name="android:icon">@drawable/splash_screen_image</item>
    </style>

    <style name="VoizyTheme.BorderButton" parent="android:Widget.Material.Button.Borderless">
        <item name="android:background">@drawable/voizy_button_ripples</item>
        <item name="android:textColor">@color/voizy_orange</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
    </style>

    <style name="VoizyTheme.Launcher" parent="VoizyTheme">
        <item name="android:windowBackground">@drawable/splash_screen_drawable</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="VoizyFloatingButtonStyle" parent="Widget.Design.FloatingActionButton">
        <item name="android:layout_margin">@dimen/fab_margin</item>
        <item name="android:scaleType">center</item>
        <item name="android:tint">@color/voizy_orange</item>
        <item name="backgroundTint">@android:color/white</item>
    </style>

</resources>
